Place optional static map fallback assets here.

- map-india-terrain.webp
  Used as a fallback for the webapp map preview when Mapbox token is missing.
  Recommended: 2400x1600 (or larger), no text labels, light/terrain style.
