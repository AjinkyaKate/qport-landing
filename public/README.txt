Place optional static map basemap assets here.

- map-india-terrain.webp
  Used as the preferred basemap for the webapp map preview (and also as a fallback when Mapbox is unavailable).
  Recommended: 2400x1600 (or larger), no text labels, no watermarks, light/terrain style with subtle rail lines.
